[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
filterwarnings =
    ignore::DeprecationWarning
addopts = -q --strict-markers
markers =
    unit: pure unit tests without external services
    integration: API / integration tests (mocked externals)
